import React from "react";
import Layout from "../components/layout";

export default function Adventures() {

    return (
        <Layout>
            <div>Adventures</div>
        </Layout>
    );
}